import { useAuth } from "@/contexts/AuthContext";import { useLocation, useNavigate } from "react-router-dom";import { Avatar, AvatarImage, AvatarFallback } from "@/components/ui/avatar";import {  BarChart3, Users, GitCompare, DollarSign, PieChart, Kanban, LogOut, HelpCircle,} from "lucide-react";import { cn } from "@/lib/utils";import { ThemeToggle } from "@/components/ThemeToggle";const NAV_ITEMS = [  { key: "/pipeline", label: "Pipeline", icon: Kanban, roles: ["admin", "sdr", "closer"] },  { key: "/marketing", label: "Marketing", icon: BarChart3, roles: ["admin", "sdr", "closer"] },  { key: "/comercial", label: "Comercial", icon: Users, roles: ["admin", "sdr", "closer"] },  { key: "/comparativo", label: "Comparativo", icon: GitCompare, roles: ["admin", "sdr", "closer"] },  { key: "/rentabilidade", label: "Rentabilidade", icon: DollarSign, roles: ["admin"] },  { key: "/consolidado", label: "Consolidado", icon: PieChart, roles: ["admin"] },  { key: "/ajuda", label: "Ajuda", icon: HelpCircle, roles: ["admin", "sdr", "closer"] },];export function AppSidebar() {  const { profile, signOut } = useAuth();  const location = useLocation();  const navigate = useNavigate();  const role = profile?.role ?? "closer";  const filtered = NAV_ITEMS.filter(item => item.roles.includes(role));  const initials = (profile?.nome ?? profile?.email ?? "U").slice(0, 2).toUpperCase();  return (    <aside className="w-56 shrink-0 bg-sidebar-background border-r border-sidebar-border flex flex-col h-screen sticky top-0">      {/* Logo */}      <div className="p-4 border-b border-sidebar-border">        <h1 className="text-base font-bold text-sidebar-foreground tracking-tight">Pena Quadros</h1>        <p className="text-[10px] text-muted-foreground">CRM</p>      </div>
